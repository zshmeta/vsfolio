import React from 'react';
import PropTypes from 'prop-types';
import { MonacoWrapper } from './Monaco.styled';

const Monaco = () => (
 <MonacoWrapper>
    Monaco Component
 </MonacoWrapper>
);

Monaco.propTypes = {};

Monaco.defaultProps = {};

export default Monaco;
