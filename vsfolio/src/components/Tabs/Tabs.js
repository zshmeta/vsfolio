import React from 'react';
import PropTypes from 'prop-types';
import { TabsWrapper } from './Tabs.styled';

const Tabs = () => (
 <TabsWrapper>
    Tabs Component
 </TabsWrapper>
);

Tabs.propTypes = {};

Tabs.defaultProps = {};

export default Tabs;
