import React from 'react';
import PropTypes from 'prop-types';
import { FormWrapper } from './Form.styled';

const Form = () => (
 <FormWrapper>
    Form Component
 </FormWrapper>
);

Form.propTypes = {};

Form.defaultProps = {};

export default Form;
