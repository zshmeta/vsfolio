import React from 'react';
import PropTypes from 'prop-types';
import { TerminalWrapper } from './Terminal.styled';

const Terminal = () => (
 <TerminalWrapper>
    Terminal Component
 </TerminalWrapper>
);

Terminal.propTypes = {};

Terminal.defaultProps = {};

export default Terminal;
