import React from 'react';
import PropTypes from 'prop-types';
import { TabsBarWrapper } from './TabsBar.styled';

const TabsBar = () => (
 <TabsBarWrapper>
    TabsBar Component
 </TabsBarWrapper>
);

TabsBar.propTypes = {};

TabsBar.defaultProps = {};

export default TabsBar;
