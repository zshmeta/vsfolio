import React from 'react';
import PropTypes from 'prop-types';
import { TitleBarWrapper } from './TitleBar.styled';

const TitleBar = () => (
 <TitleBarWrapper>
    TitleBar Component
 </TitleBarWrapper>
);

TitleBar.propTypes = {};

TitleBar.defaultProps = {};

export default TitleBar;
